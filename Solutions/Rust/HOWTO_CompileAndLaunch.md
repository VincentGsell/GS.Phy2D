  Le Cargo.toml a toutes les informations. Les commandes sont :- cargo build --release (compiler)- cargo run --release    (lancer la démo)
  - cargo test (tests unitaires)                                                                                          - cargo doc --open (documentation)

● Voici les commandes pour le projet Rust :

  cd \GS.Phy2D\Solutions\Rust                                                                       
  # Compiler en mode optimisé                                                                                             cargo build --release

  # Lancer la démo
  cargo run --release

  # Tests unitaires
  cargo test

  # Documentation
  cargo doc --open

  Le projet utilise macroquad pour le rendu graphique et rand pour les nombres aléatoires.